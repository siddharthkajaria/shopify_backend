<!-- Navbar -->
<nav class="main-header navbar navbar-expand navbar-white navbar-light">
    <!-- Left navbar links -->
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="`tton"><i class="fas fa-bars"></i></a>
        </li>
    </ul>

    <!-- Right navbar links -->
    
    <ul class="navbar-nav ml-auto">
        <li class="nav-item">
            <%# <a class="nav-link active" href="" data-controlsidebar-slide="true">
                <i class="fas fa-sign-out-alt"> Sign Out</i>
            </a> %>
            <a href="#" class="nav-link active" data-controlsidebar-slide="true" data-toggle="modal" data-target="#modal-logout">
                <i class="fas fa-sign-out-alt"></i> Sign Out
            </a>
        </li>
    </ul>

    <div class="modal fade" id="modal-logout">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h4 class="modal-title">Logout <i class="fa fa-lock"></i></h4>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <p>Are you sure you want to log-off?</p>
                </div>
                <div class="modal-footer">
                    <%# <a class="btn btn-primary" href="/logout">Logout</a> %>
                    <%= button_to "Logout", destroy_user_session_path, :method => :delete, class: "btn btn-primary" %>
                    <button type="button" class="btn btn-danger" data-dismiss="modal">Close</button>
                </div>
            </div>
        </div>
    </div>
</nav>

<style>
  .modal-backdrop{
    z-index: 11 !important;
  }
</style>
<!-- /.navbar -->